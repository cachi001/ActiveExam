## 1. Bloque 1 — Monitor: Buffer IndexedDB + drain + scorePropio

- [x] 1.1 En `useExamProctoring.ts`, agregar imports de `CircularEventBuffer` e `IndexedDbEventBufferStore` desde `../transport/eventBuffer` y `../transport/indexedDbBufferStore`. Envolver la instanciación en try/catch: si IndexedDB lanza, almacenar `null` en el ref y continuar sin buffer (degradación silenciosa).
- [x] 1.2 Crear un `bufferRef = useRef<CircularEventBuffer | null>(null)` inicializado en el useEffect de arranque (después de que la sesión esté disponible o en paralelo). El buffer persiste para toda la duración del hook.
- [x] 1.3 En el callback `handleEvent.current`, implementar el patrón **buffer-first con purga-en-éxito**: (a) ANTES del POST, `await bufferRef.current?.append(rawEvent.id, payloadDelEvento)` (idempotente por id); (b) ejecutar `api.enviarEventoProctoring`; (c) **SI el POST resuelve OK, `await bufferRef.current?.confirm(rawEvent.id)` para PURGAR el evento del buffer**; (d) si el POST rechaza (red caída), NO confirmar — el evento queda en el buffer para el drain. Silenciar errores del buffer con `.catch(() => {})`. **CRÍTICO**: sin el `confirm` on-success, el buffer retiene todos los eventos y `drainAndReplay` los reenvía masivamente en cada reconexión (el backend slim NO deduplica). Ver design D1/R2.
- [x] 1.4 Crear un adaptador `ReplaySender` inline en `useExamProctoring.ts` (~15 líneas): una función `async (event) => { await api.enviarEventoProctoring(sid, event); }` que envuelve el sink REST existente. Usar `drainAndReplay(bufferRef.current, replaySender)` importado de `../transport/replayCoordinator`.
- [x] 1.5 Agregar listener `online` en el useEffect de arranque: `window.addEventListener('online', handleDrain)`. `handleDrain` llama `drainAndReplay` con el `ReplaySender` del paso anterior y el sessionId actual (leer de `sessionIdRef.current`). Silenciar errores con `.catch(() => {})`. Nota: gracias al `confirm` on-success del paso 1.3, el buffer en este punto solo contiene los eventos que fallaron mientras la red estaba caída — el drain reenvía únicamente esos.
- [x] 1.6 Agregar listener `offline` (solo para registro de diagnóstico o future use). Limpiar ambos listeners en el return del useEffect junto con los existentes.
- [x] 1.7 En el cleanup del useEffect (el return), invocar `drainAndReplay` una última vez antes de limpiar el buffer, para enviar cualquier evento pendiente al finalizar el examen.
- [x] 1.8 Cablear `addScore`: en el callback `handleEvent.current`, importar `useApp` y llamar `store.addScore(PESO_SCORE[rawEvent.severidad as Severidad] ?? 0)`. Verificar que `store.scorePropio` crece al detectar eventos en el navegador. Nota: el hook ya usa `useApp` para `setProctoringSessionId` — agregar `addScore` al mismo selector.
- [x] 1.9 Agregar comentario de deuda técnica en `useExamProctoring.ts` indicando que `eventSignature.ts` (firma HMAC) y `evidenceCapture.ts` (cadena de custodia completa) no se cablea porque el backend slim no valida firmas ni tiene `/evidence/presign`. Referenciar los archivos con sus paths relativos.
- [x] 1.10 Ejecutar `vitest run` en la suite de transport (`frontend/src/transport/*.test.ts`) — deben seguir pasando sin modificaciones (los módulos de transport no se tocan, solo se invocan).

## 2. Bloque 2 — Biometría: Liveness pasivo real + cámara virtual + retos reales

- [x] 2.1 En `BiometricCapture.tsx`, agregar un `livenessWindowRef = useRef<Array<{ blinkL: number; blinkR: number; noseX: number; noseY: number; minZ: number; maxZ: number; frameTime: number }>>([])` con capacidad máxima N=15 frames.
- [x] 2.2 En el loop RAF `detectFrame`, después de obtener `landmarks` con `face_count > 0`, extraer los valores de la ventana: `blinkL = Math.abs(landmarks[159].y - landmarks[145].y)`, `blinkR = Math.abs(landmarks[386].y - landmarks[374].y)`, `noseX = landmarks[1].x`, `noseY = landmarks[1].y`, `minZ = Math.min(...landmarks.map(l => l.z))`, `maxZ = Math.max(...landmarks.map(l => l.z))`. Agregar al ref de ventana (pop el más viejo si length >= 15).
- [x] 2.3 Calcular las tres métricas sobre la ventana: `blinkVariance = variance([...blinkL, ...blinkR])`, `motionVariance = variance([...noseX, ...noseY])`, `depthRange = max(maxZ) - min(minZ)`. Implementar la función `variance(arr: number[]): number` inline (sum of squared deviations / n, returns 0 if n < 2).
- [x] 2.4 Invocar `derivePassiveSignals({ blinkVariance, motionVariance, depthRange })` y `passivePassed(signals)` (importar de `../vision/liveness`) y almacenar el resultado en `passiveOkRef = useRef(false)`. Actualizar `passiveOkRef.current` en cada frame.
- [x] 2.5 Agregar `prevFrameDataRef = useRef<ImageData | null>(null)` para la detección de cámara virtual. En cada frame, dibujar el bitmap en un canvas off-screen `16×12`, obtener `ImageData` y calcular la diferencia cuadrática media respecto al frame anterior (`interFramePixelVariance`). Almacenar el `ImageData` actual en `prevFrameDataRef` para el próximo frame.
- [x] 2.6 Calcular `frameRateJitter` como desviación estándar de los últimos valores de `performance.now()` entre frames en la ventana (usar `livenessWindowRef.current.map(f => f.frameTime)`). Calcular `faceCountStability` como proporción de frames en la ventana con `face_count === 1`.
- [x] 2.7 Invocar `detectVirtualCamera({ interFramePixelVariance, frameRateJitter, faceCountStability })` (importar de `../vision/liveness`) y almacenar en `virtualCameraRef = useRef(false)`. Si `detectVirtualCamera` retorna `true`, actualizar el ref.
- [x] 2.8 Ampliar la firma de `BiometricCaptureProps.onComplete` a: `(landmarks: FaceLandmark[], frame: HTMLCanvasElement | null, passiveOk: boolean, retosResueltos: string[], virtualCameraDetected: boolean) => void`.
- [x] 2.9 En `procesarCompletado()`, actualizar la llamada a `onComplete` para pasar `passiveOkRef.current`, `resueltosRef.current` y `virtualCameraRef.current`. En modo `fallbackManual` (detectado por el flag de estado), pasar `passiveOk: false` y `virtualCameraDetected: false` explícitamente.
- [x] 2.10 En `Biometria.tsx`, actualizar el handler `handleComplete` para aceptar los 5 parámetros. Reemplazar `liveness_ok: true` hardcodeado por `passiveOk`. Reemplazar `retos_resueltos: []` por `retosResueltos`. Agregar `virtual_camera_detected: virtualCameraDetected` al payload de `api.enviarBiometriaProctoring` si el tipo lo acepta (o en `payload` extra si el tipo es fijo).
- [x] 2.11 Actualizar el caller de enrollment `frontend/src/screens/enrollment/EnrollmentBiometricStep.tsx` (monta `BiometricCapture` en la fase `capturando`, `onComplete` en línea ~118): su `handleComplete` debe aceptar la firma de 5 parámetros. El enrollment ignora `passiveOk`/`virtualCameraDetected` con `_` (su liveness se evalúa durante los retos de la captura de referencia, no condiciona el guardado del descriptor). Confirmar que `tsc --noEmit` no reporta otros callers de `onComplete` sin actualizar.
- [x] 2.12 Agregar un contador `passiveFalseFramesRef = useRef(0)` en `BiometricCapture.tsx`. Si `passivePassed()` retorna `false`, incrementar. Si retorna `true`, resetear a 0. Si el contador supera 90 frames, NO bloquear — solo asegurar que al completar, `passiveOk: false` se propaga honestamente (comportamiento ya cubierto por el paso 2.9).
- [x] 2.13 Ejecutar `vitest run` para los tests de liveness (`frontend/src/vision/liveness*.test.ts` si existen) — no se modifican, solo se invocan.

## 3. Bloque 3 — Cadena de custodia: hash SHA-256 en payload del evento

- [x] 3.1 En `useExamProctoring.ts`, importar `hashClip` de `../features/biometria/clipCustody`.
- [x] 3.2 En `handleEvent.current`, después de obtener el `screenshot` (base64 string de `captureVideoFrame`), si `screenshot !== null`, calcular el hash: convertir base64 a `ArrayBuffer` via `Uint8Array(atob(screenshot.replace(/^data:[^;]+;base64,/, '')))`, luego `await hashClip(buffer)`. Envolver en try/catch — si falla, continuar sin el campo.
- [x] 3.3 Agregar `screenshot_sha256_cliente: hash` al objeto del payload pasado a `api.enviarEventoProctoring`. Si el hash es `undefined` (screenshot null o error), omitir el campo del payload (no enviar `undefined`).
- [x] 3.4 Verificar con una prueba manual o en los tests existentes que el campo `screenshot_sha256_cliente` tiene exactamente 64 caracteres hexadecimales cuando el screenshot es válido.
